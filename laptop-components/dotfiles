fancy_echo "Setting up dotfiles..."
git clone git://github.com/thoughtbot/dotfiles.git .dotfiles

fancy_echo "Running rcup"
rcup -v -x "README.md LICENSE Brewfile" -d $HOME/.laptop/dotfiles -d $HOME/.dotfiles
